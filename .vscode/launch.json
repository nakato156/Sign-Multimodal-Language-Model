{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python Debugger: Run mslm.cli",
      "type": "debugpy",
      "request": "launch",
      "module": "mslm.cli",
      "console": "integratedTerminal",
      "args": ["train", "--epochs", "10", "--batch_size", "32"],
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  ]
}
